# 代码审查报告 — Dell/Delta 750W 数控电源控制器

> 审查日期：2026-06-09
> 审查范围：全项目 12 个核心源文件
> 固件版本：v1.0 (commit d75f84d)
> **本报告仅提出建议，未对代码做任何修改。**

---

## 一、项目概况

| 项目 | 说明 |
|------|------|
| 平台 | ESP32-C3 (RISC-V 单核 160MHz) |
| 框架 | ESP-IDF v5.x, C++17 |
| 图形 | LVGL v9 (NXP GUI Guider) |
| 蓝牙 | NimBLE (Apache Mynewt) |
| 任务数 | 3 (hw_poll + ui_btn + BLE主循环) |
| 源文件 | 12 个 .cpp + 对应头文件 |
| 代码量 | 约 2500 行 |

**架构概览：**

```
app_main (BLE主循环, 100ms)
├── task_hardware_poll (prio=2, 50ms): PMBus::scan() + ADCSampler::sample()
└── task_ui_and_buttons (prio=3, 50ms): 按键处理 + LVGL刷新

数据流:
  PSU ←→ PMBus(I2C) → 静态成员 → BLE JSON / LVGL 显示
  ADC引脚 → ADCSampler → 校准插值 → 电压显示
  按键 → handle_buttons() → lvgl_ui_handle_key() → 状态机
  BLE RX → on_ble_rx() → JSON解析 → PowerControl / Calibration
```

---

## 二、严重 Bug

### Bug #1 — setVMax 电压裁剪逻辑完全失效

**文件：** [power_control.cpp:219-227](main/power_control.cpp#L219-L227)

**问题描述：**

```cpp
void PowerControl::setVMax(float vMax)
{
    // ...
    if (xSemaphoreTake(s_mutex, portMAX_DELAY) == pdTRUE) {
        _vMax = vMax;                    // ① 先更新 V_max
        if (_voltageSet > vMax) {        // ② 但 _voltageSet 还没改,条件正常
            _voltageSet = vMax;          // ③ 这里把 _voltageSet 裁到新上限
            newVoltage = vMax;
            needUpdate = true;
            ESP_LOGI(TAG, "V_set trimmed from %.3f to %.1f",
                     _voltageSet, vMax); // ④ 日志打印:两个值相同!
        }
        xSemaphoreGive(s_mutex);
    }
    // ...
}
```

**Bug 根因：** 行 ④ 的日志中 `_voltageSet` 已经在行 ③ 被赋值为 `vMax`，所以打印出来是 "trimmed from 12.0 to 12.0"，看不到旧值。

但**更严重的是行 ③ 的赋值在行 ② 的 if 体内**——如果条件成立，`_voltageSet` 确实会被正确裁剪到 `vMax`。这部分的逻辑实际上是**正确的**。

**重新审查：** 行 ② 的条件 `_voltageSet > vMax` 使用的是**传入参数 `vMax`**（不是 `_vMax`），所以判断是正确的。行 ③ 将 `_voltageSet` 裁剪为 `vMax` 也是正确的。

**真正的 Bug：** 行 224 的日志参数错误——应保存旧值再打印：

```cpp
// 建议修复：
float oldV = _voltageSet;
if (_voltageSet > vMax) {
    _voltageSet = vMax;
    // ...
    ESP_LOGI(TAG, "V_set trimmed from %.3f to %.1f", oldV, vMax);
}
```

**严重程度：** 🟡 低 — 功能正确但日志误导，不影响实际行为。

---

### Bug #2 — setIMax 同样的问题

**文件：** [power_control.cpp:249-251](main/power_control.cpp#L249-L251)

与 Bug #1 完全相同的模式。日志中旧的电流值丢失，打印两个相同值。

```cpp
_currentSet = iMax;
ESP_LOGI(TAG, "I_set trimmed from %.3f to %.1f", _currentSet, iMax);
// 打印结果: "I_set trimmed from 50.0 to 50.0"
```

**但逻辑本身正确**——`_currentSet` 会被正确裁剪到 `iMax`。仅是日志输出不准确。

**严重程度：** 🟡 低 — 同 Bug #1。

---

## 三、中等风险问题

### Issue #3 — PMBus 静态成员的数据竞争

**文件：** [main.cpp:229-249](main/main.cpp#L229-L249)

**问题描述：**

```cpp
// task_hardware_poll (50ms 周期, prio=2):
PMBus::scan();  // 写入 PMBus::I_out, V_out, W_out, temperature[] 等

// BLE 主循环 (100ms 周期):
build_full_data_json();  // 读取 PMBus::I_out, V_out, W_out, temperature[] 等
```

两个任务并发访问 PMBus 静态 float 成员。虽然 ESP32-C3 是单核，且 FreeRTOS 时间片轮转不会在单条 32 位 store 指令中打断，但问题在于：

1. **`PMBus::_readLinear()` 中的 `static float lastE_in/lastE_out`** 在函数内部有读-改-写操作，不是原子的
2. **`PMBus::_bReadMFR`** 布尔标志在 `scan()` 中被设置，在 `getInfoJson()` 中被读取

**代码自行承认了这个问题：**

```cpp
// main.cpp:247-248
/* 注意：PMBus::scan() 会将结果写入静态成员（如 PMBus::I_out）。
 * 如果 build_full_data_json() 被 BLE 主循环调用且同时本任务也在写，
 * 可能发生数据撕裂。建议后续在 PMBus 类内部使用互斥锁保护所有静态数据。 */
```

**影响：** 实际概率很低。单核环境下 32-bit float 的 store/load 是原子的，且 BLE 推送间隔（2s）远大于 scan 周期（100ms），碰撞窗口极小。

**建议：** 在 `PMBus::scan()` 内部加锁，或在 `build_full_data_json()` 读取前加锁。最简单方案：给整个 `scan()` 加一个 mutex，`build_full_data_json()` 读取前 lock、读取后 unlock。

**严重程度：** 🟡 中 — 低概率但真实存在。

---

### Issue #4 — `on_ble_rx()` 中的 JSON 解析过于脆弱

**文件：** [main.cpp:312-458](main/main.cpp#L312-L458)

**问题描述：**

当前"JSON 解析"使用 `strstr` 暴力查找字段名：

```cpp
const char* vp = strstr(line, "\"V_set\":");
if (vp) { vp = strstr(vp, ":") + 1; val = strtof(vp, nullptr); ... }
```

**问题场景：**

1. **嵌套 JSON 误匹配：** 如果传入 `{"cmd":"set","comment":"see V_set:12"}`，会在注释字符串中错误匹配
2. **字段名前缀误匹配：** 传入 `{"cmd":"set","V_set_extra":12.0}` 会匹配到错误字段
3. **没有错误恢复：** 格式损坏的 JSON 不会返回错误信息，而是静默部分执行
4. **数组/嵌套对象不支持：** 像 `calibrate` 命令已经在用 `strstr(line, "\"points\"")` 手动解析数组，对于稍复杂的结构维护成本极高

**建议：** 引入 `cJSON` 库（ESP-IDF 已自带，添加 `cJSON` 到 CMakeLists.txt 的 PRIV_REQUIRES 即可）。改动量约 50 行，但能消除一整类 bug。

**严重程度：** 🟡 中 — 当前协议简单，风险可控。随着协议变复杂风险上升。

---

### Issue #5 — `ble_server.cpp` 中 `g_connected` 等全局变量无保护

**文件：** [ble_server.cpp:41-45](main/ble_server.cpp#L41-L45)

```cpp
static uint16_t g_conn_handle   = 0;
static bool     g_connected     = false;
static bool     g_synced        = false;
static ble_rx_callback_t g_rx_callback = nullptr;
```

这些变量被 NimBLE 主机任务（GAP 事件回调）和 app_main 主任务并发访问。在 ESP32-C3 单核上，bool/uint16_t 的读写是原子的，但 `g_conn_handle` 和 `g_connected` 之间存在逻辑关联（应先设置 handle 再设 connected），多字节变量在某些场景下可能部分更新。

**当前风险：** 低。唯一可能出问题的场景是 GAP 断开事件和 `ble_server_send()` 同时执行的时间窗口极窄（纳秒级）。

**建议：** 使用 `esp_atomic_*` 或临界区保护状态切换。

**严重程度：** 🟢 低 — 单核环境，极低概率。

---

## 四、代码质量问题

### Issue #6 — `lvgl_ui.cpp` 定时器无差别刷新全部标签

**文件：** [lvgl_ui.cpp:86-120](main/lvgl_ui.cpp#L86-L120)

**问题描述：**

定时器每 500ms 触发一次，对主页面的所有标签（电压、电流、功率、温度、风扇、运行时间等）调用 `lv_label_set_text()`。即使数值未变化也是如此。

```cpp
// 每 500ms 无条件更新:
snprintf(b,sizeof(b),"%02d.%02d",ip,fp);
lv_label_set_text(u->shouye_label_49,b);  // 电压
// ... 重复 15+ 次
```

**影响：**
- `snprintf` 和 `lv_label_set_text()` 的开销很小，但格式化浮点数可能有微小波动（如 12.001 vs 12.000），导致 LVGL 内部字符串比较失败而触发不必要的重绘
- 在低优先级的 LVGL 渲染任务中可能累积延迟

**建议：** 对变化缓慢的数据（温度、风扇转速、运行时间、电能）加变化阈值检测：

```cpp
static float lastTemp = -999;
float t = PMBus::temperature[0];
if (fabsf(t - lastTemp) > 0.5f) {
    snprintf(b, sizeof(b), "%.0f", t);
    lv_label_set_text(u->shouye_label_44, b);
    lastTemp = t;
}
```

**严重程度：** 🟢 低 — 不影响功能，仅浪费少量 CPU。

---

### Issue #7 — `pmbus.cpp` 中的死代码

**文件：** [pmbus.cpp:143-147](main/pmbus.cpp#L143-L147)

```cpp
// E_in/E_out 注释掉: 该 PSU 的累计电能寄存器格式不匹配 LINEAR11, 持续产生警告日志
// E_in  = _readLinear(PMBUS_READ_EIN);
// esp_rom_delay_us(1000);
// E_out = _readLinear(PMBUS_READ_EOUT);
// esp_rom_delay_us(1000);
```

但 `_readLinear()` 函数内部有约 40 行专门针对 E_in/E_out 的验证逻辑（跳变检测、负值检测、上限检查），这些代码现在是**死代码**：

```cpp
// pmbus.cpp:408-442 — 约 35 行永远不会执行的代码
if (reg == PMBUS_READ_EIN || reg == PMBUS_READ_EOUT) {
    static float lastE_in = 0, lastE_out = 0;
    // ... 跳变检测 ...
    // ... 负值检测 ...
    // ... 上限检查 ...
}
```

**建议：** 如果确定不需要 E_in/E_out（注释说格式不匹配），应清理这段死代码。如果未来可能启用，加 `#if 0` ... `#endif` 明确标注。

**严重程度：** 🟢 低 — 不影响功能，仅增加维护困惑。

---

### Issue #8 — `scan()` 内部频率限制与调用方周期不匹配

**文件：** [pmbus.cpp:122-126](main/pmbus.cpp#L122-L126)

```cpp
static uint64_t lastScan = 0;
uint64_t now = esp_timer_get_time() / 1000;
if ((now - lastScan) < 100) return 0;  // 100ms 最小间隔
lastScan = now;
```

`task_hardware_poll` 以 50ms 周期调用 `scan()`，意味着**一半的调用是空操作**（立即返回 0）。

**建议：** 二选一：
- 将 hw_poll 周期改为 100ms（PMBus 数据变化本身就慢）
- 移除 scan() 内部的频率限制，由调用方控制节奏

**严重程度：** 🟢 低 — 浪费一次函数调用（微秒级）。

---

### Issue #9 — `ble_server_send()` 使用 VLA (变长数组)

**文件：** [ble_server.cpp:235](main/ble_server.cpp#L235)

```cpp
size_t CHUNK_SIZE = (mtu > 4) ? (size_t)(mtu - 4) : 19;
char chunk[CHUNK_SIZE + 2];  // VLA，非C++标准
```

VLA (Variable Length Array) 是 C99/GCC 扩展，不是 C++ 标准。在某些编译器或模式下（如 `-Wvla`）会报警。

**影响：** ESP-IDF 使用 GCC，支持 VLA。当前无实际问题。

**建议：** 改用固定 512 字节缓冲区（MTU 通常 ≤ 512），或使用 `alloca()` 明确表达意图。

**严重程度：** 🟢 低 — 可移植性问题，不影响 ESP32-C3。

---

### Issue #10 — `portMAX_DELAY` 可能导致死锁

**文件：** 多处使用

所有 getter 方法使用 `portMAX_DELAY` 等待互斥锁：

```cpp
// power_control.cpp:281
if (xSemaphoreTake(s_mutex, portMAX_DELAY) == pdTRUE) { v = _voltageSet; xSemaphoreGive(s_mutex); }
// adc_sampler.cpp:267
if (xSemaphoreTake(_mutex, portMAX_DELAY) == pdTRUE) { v = _calibratedVoltage; xSemaphoreGive(_mutex); }
```

`portMAX_DELAY` 意味着如果锁被持有且永不释放，任务将**永久阻塞**。在单核 ESP32-C3 上，如果持有锁的任务因为异常（如 I2C 总线错误导致死等）而不释放锁，所有等待该锁的任务都会卡死。

**建议：** 改用有限超时（如 `pdMS_TO_TICKS(100)`），超时后返回默认值并记录错误：

```cpp
if (xSemaphoreTake(s_mutex, pdMS_TO_TICKS(100)) == pdTRUE) {
    v = _voltageSet;
    xSemaphoreGive(s_mutex);
} else {
    ESP_LOGE(TAG, "Mutex timeout!");
    return 0.0f;  // 安全默认值
}
```

**严重程度：** 🟡 中 — 正常情况下不会触发，但一旦触发就是死锁。

---

## 五、架构与设计建议

### 建议 #11 — 统一 NVS 命名空间

当前项目使用了 **4 个不同的 NVS 命名空间**：

| 命名空间 | 使用者 | 用途 |
|----------|--------|------|
| `power_ctrl` | power_control.cpp | V_set/I_set/V_max/I_max |
| `adc_cal` | adc_sampler.cpp | ADC 零点偏移 |
| `calib_data` | calibration.cpp | 6 点校准表 (电压/电流/ADC) |
| `calibration` | pmbus.cpp | 电流校准表 |

建议统一到一个命名空间或使用 `nvs_` 子命名空间约定，便于管理和擦除。

---

### 建议 #12 — `calibration.cpp` 与 `pmbus.cpp` 校准表职责重叠

存在两套电流校准机制：

1. **calibration.cpp 的 6 点 PWM 校准表** — 用于 ESP32 → DAC → PSU 的模拟控制
2. **pmbus.cpp 的 6 点电流校准表** — 用于修正 PMBus 电流读数的误差

两者的目标数组**数值不同**：
- calibration: `{0, 10, 20, 30, 50, 62.5}`
- pmbus: `{0, 5, 15, 30, 45, 60}`

这是有意为之（前者校准 PWM 输出，后者校准 PMBus 读数），但命名和位置容易混淆。建议在头文件中增加注释明确说明两者的关系。

---

### 建议 #13 — 错误处理不够完善

**观察：** 很多函数的返回值未被检查：

```cpp
// adc_sampler.cpp:187
adc_oneshot_read(_adcHandle, _adcChannel, &raw);  // 返回值被忽略
// pmbus.cpp
_readLinear(PMBUS_READ_TEMPERATURE_1);  // 失败时返回 0.0f, 无法区分"读失败"和"真为0"
```

对于传感器读取，失败时返回 0 是合理的 fallback。但对于关键操作（如 `setVoltage` 中的 PWM 设置），如果 `ledc_set_duty` 失败，当前没有反馈给调用方。

---

### 建议 #14 — 缺少看门狗

ESP32-C3 支持硬件看门狗（IWDT 和 TWDT）。当前代码有三个 FreeRTOS 任务，如果其中一个任务死循环或阻塞，没有自动恢复机制。建议：

- 为每个任务注册 TWDT 用户
- 在任务循环中定时 `esp_task_wdt_reset()`

---

## 六、总结

### 按严重程度统计

| 严重程度 | 数量 | 说明 |
|----------|------|------|
| 🔴 严重 | 0 | 无会导致死机或功能完全失效的 bug |
| 🟡 中等 | 5 | 数据竞争、JSON 解析脆弱、日志 bug、死锁风险、频率不匹配 |
| 🟢 低 | 8 | 死代码、VLA、可移植性、NVS 命名、缺少看门狗等 |

### 按模块统计

| 模块 | 发现数 | 最严重 |
|------|--------|--------|
| power_control.cpp | 2 | Bug #1 (日志) |
| pmbus.cpp | 3 | Issue #3 (数据竞争) |
| main.cpp | 2 | Issue #4 (JSON解析) |
| ble_server.cpp | 2 | Issue #5 (无保护变量) |
| lvgl_ui.cpp | 1 | Issue #6 (刷新优化) |
| adc_sampler.cpp | 1 | Issue #10 (死锁风险) |
| calibration.cpp | 1 | 建议 #12 |

### 整体评价

**代码质量：中等偏上 (7/10)**

- ✅ **优点：**
  - 安全开机时序设计严谨（SW_CTRL 先低后高原则）
  - 校准算法正确（6 点分段线性插值，与 PMBus 电流校准独立）
  - UI 状态机清晰（页面切换、焦点管理、编辑模式）
  - BLE 分包发送考虑了 MTU 动态协商
  - 代码注释真诚，甚至自行指出了已知问题
  - ADC 采样链路设计合理（过采样→移动平均→插值校准→EMA 平滑）

- ⚠️ **短板：**
  - 缺少统一的错误处理框架
  - JSON 解析过于原始
  - 互斥锁使用 portMAX_DELAY，缺少超时保护
  - PMBus 数据缺少互斥保护（代码注释已承认）
  - 部分死代码未清理

**总体而言**，这是一个功能完整、设计合理的嵌入式固件项目。建议优先处理 Issue #3（PMBus 数据竞争）和 #10（死锁风险），其次是 #4（JSON 解析）。其余问题可以随版本迭代逐步改进。
