# 角色设定
你现在是一位资深的嵌入式软件工程师（C/C++），精通 ESP32-C3 芯片（Arduino 框架或 ESP-IDF）。你需要为我刚刚设计完成的一款“台达/戴尔 750W 数控电源改造控制板”编写底层驱动代码（BSP）。

# 硬件平台概述
- **主控芯片**：ESP32-C3 (QFN32 封装)
- **核心功能**：通过单片机控制服务器电源的开关、通过硬件 PWM+RC+LM358 模拟 DAC 控制电源的输出电压和电流、通过 ADC 采样外部电压、通过 PMBus(I2C) 读取电源内部状态。
- **UI 交互**：3 个物理按键 + SPI 屏幕。

# 第一部分：全局 GPIO 引脚映射表 (Pin Map)
请在代码中使用宏定义（#define）严格绑定以下引脚：
- V_ADC_IO0 = 0   // 模拟输入，用于外部 12V 降压采样
- SW_CTRL = 1     // 数字输出，控制电源开机 (HIGH = 开机, LOW = 关机)
- V_PWM = 2       // PWM 输出 (LEDC)，控制输出电压
- I_PWM = 3       // PWM 输出 (LEDC)，控制输出电流
- PMBUS_SCL = 4   // I2C 时钟线 (读取原厂电源数据)
- PMBUS_SDA = 5   // I2C 数据线
- TFT_SCLK = 6    // SPI 屏幕时钟
- TFT_MOSI = 7    // SPI 屏幕数据
- TFT_RST = 8     // 屏幕硬件复位
- TFT_DC = 9      // 屏幕数据/命令选择 (注意：这是 GPIO9 BOOT 脚，开机需保持高电平)
- TFT_CS = 10     // 屏幕片选
- BTN_UP = 18     // 按键 (需内部上拉，低电平有效)
- BTN_DOWN = 19   // 按键 (需内部上拉，低电平有效)
- BTN_OK = 20     // 按键 (需内部上拉，低电平有效)

# 第二部分：核心数学模型与控制逻辑（极其重要，涉及硬件安全！）

## 1. PWM 转 DAC 基础参数
- 单片机逻辑电压 (MCU_VDD) = 3.3V
- 目标 DAC 控制电压上限 (V_DAC_MAX) = 3.0V
- PWM 分辨率建议使用 13-bit (0-8191)，频率设定为 20kHz。

## 2. 电压控制环路 (反向逻辑 / Inverse Logic)
- **硬件逻辑**：当 DAC 输出 3.0V 时，电源输出 0V；当 DAC 输出 0V 时，电源满载输出 12.0V。
- **数学公式**：`V_DAC = 3.0 - (Target_Voltage / 12.0) * 3.0`
- **安全警告**：由于是反向逻辑，PWM 占空比为 0 时输出最高电压！

## 3. 电流控制环路 (正向逻辑 / Direct Logic)
- **硬件逻辑**：当 DAC 输出 0V 时，电源限流 0A；当 DAC 输出 3.0V 时，电源满载输出 62.5A。
- **数学公式**：`I_DAC = (Target_Current / 62.5) * 3.0`

## 4. 外部 ADC 电压采样 (分压还原)
- **硬件逻辑**：外部 0~12V 电压经过 20.47kΩ 和 5kΩ 电阻分压后进入 ADC。
- **数学公式**：`Real_Voltage = ADC_Read_Voltage * ((20.47 + 5) / 5) = ADC_Read_Voltage * 5.094`
- **处理要求**：请实现一个包含 10 次采样的移动平均滤波算法（Moving Average Filter）来平滑数据。

# 第三部分：强制开机时序与安全策略 (Boot Sequence)
为了防止单片机复位或上电瞬间炸毁外接负载，`setup()` 函数必须严格按照以下顺序执行：
1. **第一步（锁定命门）**：立即将 `SW_CTRL` (GPIO1) 配置为 OUTPUT 并输出 LOW（切断电源输出）。
2. **第二步（安全偏置）**：立即初始化 PWM（LEDC），将 `V_PWM` 输出其对应的最高占空比（约 90.9%，即 3.0V，对应 0V 物理输出），将 `I_PWM` 输出 0 占空比。
3. **第三步（延时稳定）**：等待至少 50ms，让 RC 滤波器的电容充电稳定。
4. **第四步**：初始化其他外设（I2C, SPI, 按键等）。

# 任务要求
请根据以上所有要求，使用 C++ 为我编写一套面向对象风格（OOP）的底层驱动类代码。
我需要以下模块结构：
1. `PowerControl` 类：封装开/关机、设定电压、设定电流的接口，隐藏内部的数学转换逻辑。
2. `ADCSampler` 类：封装 ADC 的读取、分压还原计算和滤波算法。
3. 给出包含安全时序的 `setup()` 函数示例。

代码要求注释清晰，命名规范，不要写死（Hardcode）魔法数字，尽量使用 `const` 定义常量。  
通信1.md和通信2.md文件是IIC通信源码请参考