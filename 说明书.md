# Dell/Delta 750W 数控电源控制器 — 使用说明书

## 一、产品概述

本设备是一台基于 ESP32-C3 的数控可调电源控制器，专门用于驱动**台达/戴尔 750W 服务器电源**（型号 DPS-750AB 及兼容型号），将其改造为 0~50V / 0~100A 数控可调直流电源。

**核心功能：**
- 本地控制：2.01 寸 TFT 彩色屏幕 + 3 个实体按键
- 远程控制：手机 APP 通过蓝牙 BLE 连接控制
- 6 点精密校准：电压/电流独立校准，分段线性插值
- 实时监测：电压、电流、功率、电能、温度、风扇转速

---

## 二、硬件规格

| 项目 | 参数 |
|------|------|
| 主控芯片 | ESP32-C3 (RISC-V 单核 160MHz) |
| 屏幕 | 2.01 寸 ST7789P3 TFT，240×296 分辨率 |
| 通信接口 | BLE 4.2 (NimBLE)，Nordic UART Service |
| 电压量程 | 0 ~ 50.0V（默认上限 12.0V，可修改） |
| 电流量程 | 0 ~ 100.0A（默认上限 62.5A，可修改） |
| 额定功率 | 750W |
| 按键 | 3 键：上(UP)、下(DOWN)、确认(OK) |
| 供电 | 由 PSU 待机电源供电 |

---

## 三、开机与安全

### 3.1 安全开机时序（固件自动执行）

1. 锁定 SW_CTRL 为低电平（切断输出）
2. V_PWM、I_PWM 初始化输出 0（DAC 电压 = 0V）
3. 延时 50ms 等待 RC 滤波器稳定
4. 初始化所有外设（I2C、ADC、BLE、屏幕）
5. 从 NVS 加载上次保存的电压/电流设定值
6. 恢复 PWM 偏置（使用校准表）

### 3.2 首次使用

首次开机后，**需要先进行电压和电流校准**，否则设定的电压/电流与实际输出会有较大偏差。

---

## 四、界面说明

### 4.1 主页面 (首页)

```
 ┌──────────────────────────────┐
 │  12.03 V     5.02 A          │  ← 实时电压/电流
 │  060.5 W   012.34 Wh         │  ← 功率/累计电能
 │  Fan: 4500  T: 35 38 42      │  ← 风扇转速/三路温度
 │                              │
 │  ┌────────────────────┐      │
 │  │  VSET    12.00 V   │      │  ← 设定电压 (焦点框)
 │  └────────────────────┘      │
 │  ┌────────────────────┐      │
 │  │  ISET    50.00 A   │      │  ← 设定电流 (焦点框)
 │  └────────────────────┘      │
 │                              │
 │  ╔════════════════════╗      │
 │  ║       ON / OFF    ║      │  ← 电源开关按钮 (焦点框)
 │  ╚════════════════════╝      │
 │                              │
 │  00:15:30                    │  ← 累计运行时间
 └──────────────────────────────┘
```

### 4.2 菜单页

长按 OK 键进入菜单，共 5 项：

| 序号 | 菜单项 | 功能 |
|------|--------|------|
| 1 | 上限设置 | 设置电压/电流量程上限 |
| 2 | 电压校准 | 进入 6 点电压校准流程 |
| 3 | 电流校准 | 进入 6 点电流校准流程 |
| 4 | 电源信息 | 查看 PSU 厂商信息和规格 |
| 5 | 返回首页 | 返回主页面 |

### 4.3 电源信息页

显示从 PSU 内部 PMBus 寄存器读取的厂商信息：
- 制造商（自动识别品牌：台达Delta / 雅达Astec / 光宝 / 康舒 / 群光 / 高效 / Flex）
- 型号、版本号、生产地点、生产日期、序列号
- 电压上限、电流上限、功率上限

---

## 五、按键操作指南

### 5.1 主页面操作

| 操作 | 功能 |
|------|------|
| 按 **UP** | 向上移动焦点 |
| 按 **DOWN** | 向下移动焦点 |
| **短按 OK**（焦点=开关） | 切换电源 ON/OFF |
| **短按 OK**（焦点=VSET） | 进入电压编辑模式 |
| **短按 OK**（焦点=ISET） | 进入电流编辑模式 |
| **长按 OK**（2秒） | 进入菜单页 |

### 5.2 数字编辑模式

进入编辑模式后，边框变为**紫色(0xFF00FF)**，数值开始**闪烁**：

| 操作 | 功能 |
|------|------|
| 按 **UP** | 当前位数字 +1 |
| 按 **DOWN** | 当前位数字 -1 |
| **短按 OK** | 切换到下一位（个位→十位→十分位→百分位→个位...） |
| **连续短按 OK**（<400ms 双击） | **确认并退出编辑**，使新值生效 |
| **长按 OK** | 取消编辑，恢复原值 |

**编辑规则：**
- 设定电压不能超过当前电压上限
- 设定电流不能超过当前电流上限
- 编辑期间值暂存在内存中，**只有双击确认才生效**

### 5.3 菜单页操作

| 操作 | 功能 |
|------|------|
| 按 **UP/DOWN** | 移动光标 `>` 选择菜单项 |
| **短按 OK** | 进入选中项目 |
| **长按 OK** | 返回主页面 |

### 5.4 上限设置页

设置电压和电流的量程上限，修改后会自动关联调整：

| 操作 | 功能 |
|------|------|
| 按 **UP/DOWN** | 移动焦点（V上限 / I上限 / 确认返回） |
| **短按 OK**（焦点=值） | 进入编辑模式 |
| **短按 OK**（焦点=确认返回） | 返回菜单 |

**量程互锁规则（750W 额定功率保护）：**
- 修改电压上限后，自动计算电流上限 ≤ 750W ÷ 电压上限
- 修改电流上限后，自动计算电压上限 ≤ 750W ÷ 电流上限
- 当前设定值超过新上限时自动裁剪

### 5.5 校准页操作

| 操作 | 功能 |
|------|------|
| 按 **UP** | PWM +2 |
| 按 **DOWN** | PWM -2 |
| **短按 OK** | 确认当前步骤，进入下一步 |
| **长按 OK** | 停止校准，返回菜单 |

校准过程共 6 步，每步有一个目标值，需要配合**万用表/电子负载**实际测量来调 PWM。

---

## 六、手机 APP 连接 (BLE)

### 6.1 连接方式

1. 设备上电后自动开始 BLE 广播
2. 广播名格式：`Dell-PSU-Controller-XXYYZZ`（MAC 后 3 字节，用于区分同型号设备）
3. 使用配套 APP 搜索并连接
4. 通信协议：JSON over BLE UART（Nordic UART Service）

### 6.2 APP 通信协议

#### 请求命���（APP → 设备）

```json
// 获取遥测数据
{"cmd":"get_data"}

// 获取设备信息
{"cmd":"get_info"}

// 设置电压
{"cmd":"set","V_set":12.0}

// 设置电流
{"cmd":"set","I_set":50.0}

// 开关电源
{"cmd":"set","power":1}    // 1=开, 0=关

// 设置量程
{"cmd":"set_range","v_max":30.0,"i_max":60.0}

// 进入/退出校准模式
{"cmd":"cal_mode","enter":1}    // 1=进入电压校准, 0=退出

// PWM 调整（校准中）
{"cmd":"cal_pwm_adjust","dir":1}    // 1=+, -1=-, 0=确认

// 确认当前校准步骤
{"cmd":"cal_confirm"}

// 设置电流校准表
{"cmd":"set_i_cal_table","points":[...]}

// 清除故障
{"cmd":"clear_faults"}
```

#### 响应格式（设备 → APP）

遥测数据响应：
```json
{
  "V_out":12.034, "I_out":5.021, "V_in":230.5, "I_in":0.85,
  "W_out":60.5, "W_in":65.2, "E_out":12.5, "E_in":13.1,
  "V_ext":12.1, "temperature":[35.2, 38.1, 42.0], "fan_speed":4500,
  "power_on":1, "V_set":12.000, "I_set":50.000,
  "V_mult":1.0000, "V_offset":0.0000, "I_mult":1.0000, "I_offset":0.0000,
  "V_raw":12.120,
  "device_online":true,
  "cal_mode":0, "cal_step":0, "cal_target":0.00, "cal_adc":0, "cal_pwm":0,
  "V_max":50.0, "I_max":100.0
}
```

设备信息响应：
```json
{
  "MFR_ID":"DELTA", "MFR_MODEL":"DPS-750AB",
  "MFR_REVISION":"A00", "MFR_LOCATION":"China",
  "MFR_DATE":"20230101", "MFR_SERIAL":"CN123456789",
  "pmbus_revision":0
}
```

### 6.3 数据推送策略

- 连接后 500ms 首次立即推送
- 后续每 2 秒推送一次
- **交替推送**：奇数秒推送设备信息（APP 信息页自动更新），偶数秒推送遥测数据
- 断开连接后停止推送

---

## 七、校准指南

### 7.1 为什么需要校准

MCU 输出 PWM 经过 RC 滤波器转为模拟电压（0~3.0V），电源将其映射为 0~50V / 0~100A。但由于元件公差和 RC 非线性，理论公式与实际输出存在误差。

校准通过**6 点分段线性插值**，在每个目标值记录对应的 PWM 值，实现精确控制。

### 7.2 电压校准步骤

**准备工具：** 数字万用表（电压档，精度 ≥ 0.1%）

1. 进入菜单 → 选择"电压校准"
2. 电源自动开机
3. Step 1/6 — 目标 0.00V，PWM=0
4. Step 2/6 — 目标 3.00V：调节 PWM 使万用表读数 ≈ 3.00V，按 OK 确认
5. Step 3/6 — 目标 5.00V：同上
6. Step 4/6 — 目标 8.00V：同上
7. Step 5/6 — 目标 10.00V：同上
8. Step 6/6 — 目标 12.00V：同上
9. 6 步完成后自动保存到 NVS 闪存

> **技巧：** 屏幕显示当前 PWM 值和 ADC 读数，帮助判断调节方向。

### 7.3 电流校准步骤

**准备工具：** 电子负载 或 大功率电阻 + 钳形电流表

1. 进入菜单 → 选择"电流校准"
2. 系统自动将电压设为 2.7V（降低功率避免过热）
3. Step 1/6 — 目标 0.00A，PWM=0
4. Step 2/6 — 目标 10.00A：调节 PWM 使电流表读数 ≈ 10.00A，按 OK 确认
5. Step 3/6 — 目标 20.00A：同上
6. Step 4/6 — 目标 30.00A：同上
7. Step 5/6 — 目标 50.00A：同上
8. Step 6/6 — 目标 62.50A：同上
9. 6 步完成后自动保存

> **注意：** 电流校准时电压自动设为 2.7V（低电压大电流），接线务必牢靠。

### 7.4 复位校准数据

如需恢复默认（使用理论公式），重新执行校准流程并保存即可覆盖旧数据。

---

## 八、LEDC PWM / DAC 参数

| 参数 | 值 | 说明 |
|------|-----|------|
| PWM 频率 | 15 kHz | 12-bit 分辨率上限为 19.531 kHz |
| PWM 分辨率 | 12-bit | 0 ~ 4095 |
| MCU 逻辑电压 | 3.3V | ESP32-C3 |
| DAC 控制电压上限 | 3.0V | 对应 100% 输出 |
| 3.0V 对应的 PWM | 3723 | (3.0/3.3) × 4095 |
| ADC 分压比 | 12.372:1 | (470+10k+10k+1.8k) / 1.8k |

---

## 九、故障排除

| 现象 | 可能原因 | 解决方法 |
|------|---------|---------|
| 屏幕不亮 | SPI 连接问题 | 检查排线，确认引脚映射正确 |
| 电压/电流显示为 0 | PSU 未连接或 PMBus 通信失败 | 检查 I2C 连接，确认 PSU 供电正常 |
| 设定电压与实际输出偏差大 | 未校准或校准数据丢失 | 重新执行电压校准 |
| BLE 无法连接 | 手机蓝牙未开启或距离过远 | 靠近设备，检查广播名是否正确 |
| 数据卡死不变 | PMBus scan 频率异常 | 重启设备 |
| 电源无法开机 | SW_CTRL 引脚连接问题 | 检查 GPIO1 连接 |
| 编辑模式下数字乱跳 | 按键去抖异常 | 重启设备 |
| NVS 读取失败 | 闪存损坏或首次开机 | 固件自动擦除并重建，正常现象 |

---

## 十、技术规格

| 项目 | 参数 |
|------|------|
| 固件 | ESP-IDF v5.x, C++17 |
| 图形库 | LVGL v9 (NXP GUI Guider 设计) |
| 蓝牙协议栈 | NimBLE (Apache Mynewt) |
| 操作系统 | FreeRTOS |
| 任务架构 | 3 任务：hw_poll(prio=2), ui_btn(prio=3), BLE main loop |
| PMBus 频率 | 100 kHz I2C |
| 遥测刷新率 | 10 Hz (100ms 间隔) |
| UI 刷新率 | 2 Hz (500ms 间隔) |
| BLE 推送间隔 | 2s（交替推送遥测+设备信息） |
| BLE MTU | 自动协商，最高 512 字节 |
| ADC 采样 | 4x 过采样 → 3点移动平均 → 6点插值校准 → EMA平滑 → 死区滞回 |

---

## 十一、安全警告

> ⚠️ **高压危险！** 服务器电源的输出可达 50V/100A，足以造成严重伤害或火灾。
>
> ⚠️ **请务必在断电状态下进行接线操作。**
>
> ⚠️ **首次使用前请确认所有接线正确、牢靠，无短路风险。**
>
> ⚠️ **不要在无人值守的情况下以高功率长时间运行。**
>
> ⚠️ **校准期间务必使用可靠的外部测量设备（万用表/电子负载）作为参考。**
>
> ⚠️ **750W 额定功率互锁保护仅在软件层面，不要依赖它作为唯一安全措施。**

---

## 十二、版本历史

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-06 | v1.0 | 初始版本：6点校准、BLE APP、LVGL v9 仪表盘、FreeRTOS 多任务 |
